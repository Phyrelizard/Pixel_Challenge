

v26.6.0
 1. DMX Editor: Added "End Trigger With Color" dropdown (RED, GREEN, BLUE, BLACK, WHITE) to Trigger Settings
 2. DMX Editor: Added "Fade To" checkbox — enables a quick fade to the selected end-trigger color
 3. DMX Editor: New fields persisted in scene JSON (end_trigger_color, end_trigger_fade)
 4. DMX Editor version updated to v1.6.0
 5. Console: Pattern effects now animate at runtime (pulse, chase, sweep, strobe, bounce, alternating, palette_cycle, random_flash, fade_loop, sparkle, breathing, wave_center, wave_lr, wave_player, build_up, explosion)
 6. Console: Scene pattern data preserved when loading from dmx_scenes.json (no longer flattened to static)
 7. Console: Scene animation loop (_scene_anim_tick) drives animate_scene_step() with speed-slider-controlled intervals
 8. Console: Selected DMX theme now applied on gameplay start (was previously ignored)
 9. Console: Editor TEST/APPLY buttons now trigger animation preview via on_scene_applied_callback
 10. Console version updated to v26.6.0
 11. Syntax check all files pass


CONSOLE 
v26.5.5

Changes Made
1. Effects dropdown now works (pixel_challenge_console_v26.0.0.py)

DMXService.apply_scene_data() and test_scene() now read scene_obj.pattern["type"] and apply the effect (strobe sets strobe channel, pulse/chase/sweep/bounce/alternating modulate dimmers)
Added animate_scene_step() method for continuous animation of patterns (chase moves active fixture, pulse oscillates, sweep slides, etc.)
Supports: static, strobe, pulse, chase, sweep, bounce, alternating, palette_cycle, random_flash, fade_loop, sparkle, breathing
2. Saved colors right-click context menu (dmx_editor.py)

Right-click on any saved color tile shows popup with: Copy, Paste Before (inserts before selected tile), Delete
Paste disabled when nothing copied; capacity capped at 16
3. Scene Preview buttons fixed (dmx_editor.py)

PREVIEW SELECTED: Now applies step colors (non-empty ones) to fixture targets cyclically
GO LIVE: Fixed to use _collect_scene_data() + apply_scene_data() instead of broken apply_scene(DMXScene_obj) call
4a. Idle wash color chooser (pixel_challenge_console_v26.0.0.py)

Click the swatch or CHANGE COLOR button → opens color picker dialog
Updates swatch, label, and the warm_amber scene definition in DMXService
APPLY WASH sends the chosen color to all fixtures
4b. Layout swap + border + preview button (pixel_challenge_console_v26.0.0.py)

Swapped: now GAMEPLAY PRESETS | IDLE WASH | RESULTS PRESETS
Results Presets has solid 2px border with highlight
PREVIEW button moved inside the bordered section with green active-state indicator
5. Editor scene highlight on open (dmx_editor.py + pixel_challenge_console_v26.0.0.py)

Editor now accepts current_scene_name parameter
Console passes dmx.current_scene to editor
Editor auto-selects and highlights matching scene in the left pane

v26.5.2
 Phase 1: Console DMX control quick wins (scene dropdown, slot buttons, speed slider, bank buttons)
 Phase 2: Editor wiring (assign button persistence, step editor click-to-edit, bank selection)
 Phase 3a: Slot button sync (editor → console via _load_slot_assignments)
 Phase 3b: Fix _assign_to_button() missing _mark_dirty() call
 Phase 3c: Results presets — animated DMX (Rainbow Rotate, Color Strobe, Chase Random) with DMXService.animate_step() + timer loop
 Phase 3d: OVERRIDE buttons on fixture cards — wired to colorchooser.askcolor() dialog
 Phase 3e: PREVIEW button — wired to apply current scene dropdown selection
 Phase 3f: Stop animations on BLACKOUT/GAMEPLAY/RESULTS/WASH/TEST/scene select/slot press
 Phase 3g: Speed slider controls animation tick interval (0=slow 500ms, 100=fast 50ms)
 Syntax check all files pass


v26.5.0

 1. Fix fixture mirroring bug: per-fixture color array _fixture_colors[16] for independent F1–F16 colors
 2. Fix fixture selection bug: clicking a fixture selects it, loads its color into wheel, changes apply only to active fixture
 3. Add ✕ CLOSE button on HSV color wheel panel
 4. Editable assign buttons with dynamic colors + double-click to rename
 5. Merged Triggers into single TRIGGER SETTINGS section
 6. Dynamic console DMX Control slot button colors
 7. Editor version v1.5.0
 8. Syntax check all files — clean
 9. Fixed duplicate append bug in assign buttons (code review feedback)
 10. Extracted module-level constants: EDITOR_VERSION, SCROLLBAR_WIDTH, USER_SLOT_COLORS (code review feedback)

v26.4.0

 1. Replace game filter buttons in left panel with a dropdown menu (same font)
 2. Rename "DUPE" → "COPY" and "DELETE" → "DEL" in left panel bottom buttons
 3. Add "NEW" button in top bar between game dropdown and SAVE button — opens name dialog, creates scene
 4. Replace Named Presets grid with a dropdown menu (colored swatch indicator). Moved Saved Colors + Lighting Effect into COLOR PALETTE section
 5. Add Event Timeline section (ATTRACT → CHECK-IN → INTRO → COUNTDOWN → GAMEPLAY → RESULTS → IDLE) next to playback nav, above Scene Preview
 6. Assign Scene to Button moved to its own full-width row: SCORE, INTRO, GAMEPLAY, START, TEST + 6 blank buttons extending to screen width
 7. Shortened Name entry field in Trigger Settings (width=22 instead of fill-expand)
 8. Added editor version label "v1.4.0" to lower right corner next to HELP button
 9. Doubled scrollbar widths (30px wide tk.Scrollbar) for touchscreen fat fingers
 10. Syntax check passed with python3 -m py_compile
 11. Help text updated to reflect all layout changes
 12. Parallel validation passed — no code review issues, no CodeQL alerts




V26.1.0

Summary
Files Changed
1. pixel_challenge_console_v25.5.0.py (NEW — 4328 lines, based on v25.4.1)

Updated docstring and VERSION_LABEL to "v25.5.0"
Added DMX_SCENES_FILE and DMX_SAVED_COLORS_FILE constants
In build_dmx_area(): removed _dmx_edit() stub and EDIT button from quick button row; added full-width purple EDITOR button row (a3)
Added three new methods: open_dmx_editor(), on_editor_closed(), open_dmx_hw_config_from_editor()
2. dmx_editor_data.py (UPDATED — 569→632 lines)

SCENE_CATEGORIES: added "wash", "fault", "victory"
GAME_FILTERS: added "ascend"
PATTERN_TYPES: added 7 new patterns (wave_center, wave_lr, wave_player, build_up, explosion, sparkle, breathing)
Added TRIGGER_BEHAVIOR_MODES and FIXTURE_ROLES constants
DMXScene.default(): added safety dict (with unit comments), trigger_behavior_map, fixture_roles
DMXScene.__init__/to_dict/validate(): updated for all new fields
SceneValidator.validate(): added safety advisory warnings
3. dmx_editor.py (UPDATED — 1629→2130 lines)

Fixture grid: 2×8 (16 fixtures F1–F16), ALL/NONE buttons, fixture control row (COLOR/REVERSE/SHIFT L/SHIFT R/MIRROR + intensity slider)
New right panel sections: TRANSITION RULES, DMX SETTINGS, SAFETY & LIMITS
Triggers: per-trigger behavior combobox, Copy/Paste Triggers buttons
Button assignment: updated labels (SCORE/INTRO/GAMEPLAY/START/TEST), right-click context menu
Playback animation: real _preview_timer_id cycling with play/rewind/fast/loop
Keyboard shortcuts: Ctrl+S (save), Ctrl+D (duplicate), Ctrl+Z (undo), Escape (close)
1-step undo: _undo_snapshot stored on scene load/save
Dirty indicator: * in breadcrumb, warn-on-close dialog
Scene sorting: Name/Category/Game sort buttons above scene list
Overwrite confirm: prompt before replacing an existing scene with a different name
WARM/COOL buttons: color temperature shifts in COLOR PALETTE section
Scoped scrolling: module-level _scroll_owner guard prevents multiple scrollable frames from interfering
ASCEND game filter: fixed key from "custom" to "ascend"

